
<div class="row justify-content-center">
  <div class="col-4">

    <%= simple_form_for [@relationship_type ] do |form| %>
      <%= form.input :forward_name, placeholder: "Name", label: false %>
      <%= form.input :backward_name, placeholder: "Backward Name", label: false %>

      <%= form.submit "save", class: "btn-solid btn-block" %>
      <% end %>
  </div>
</div>
  <!-- [...] a

_
